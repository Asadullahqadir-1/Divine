{
  "name": "divine-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm install --prefix web",
    "build": "npm run build --prefix web",
    "dev": "npm run dev --prefix web -- -p 3000",
    "start": "npm run start --prefix web",
    "typecheck": "npm run typecheck --prefix web"
  },
  "dependencies": {
    "next": "15.3.2"
  }
}